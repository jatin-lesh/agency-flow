// WhatsApp integration is not active.
// This stub keeps imports from breaking while the feature is pending.

export async function sendWhatsApp(_to: string, _message: string) {
  return { skipped: true };
}
